import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // Ensure base path is correct for GitHub Pages if needed, usually './' helps
  base: './'
})
